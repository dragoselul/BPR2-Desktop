<Window x:Class="BPR2_Desktop.Views.Windows.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Confirmation" Height="150" Width="300"
        WindowStartupLocation="CenterOwner"
        ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
        ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        WindowStyle="None" AllowsTransparency="True">
    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <StackPanel Margin="10">
            <TextBlock Text="Do you really want to go back to Home?" Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Yes" Width="75" Margin="0,0,130,0" Click="YesButton_Click"/>
                <Button Content="No" Width="75" Click="NoButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>