<ui:FluentWindow x:Class="BPR2_Desktop.Views.Pages.InputDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="{Binding Prompt}" Height="150" Width="400"
                 WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Please enter the design name:" />
            <TextBox x:Name="InputTextBox" Margin="0,10,0,10" Text="{Binding ResponseText, UpdateSourceTrigger=PropertyChanged}"
                     x:FieldModifier="public" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Appearance="Primary" x:Name="OKButton" Content="OK" Width="75" IsDefault="True" Click="OKButton_Click"
                           IsEnabled="{Binding IsInputValid}" x:FieldModifier="public" />
                <ui:Button Appearance="Dark" Content="Cancel" Width="75" IsCancel="True" Margin="10,0,0,0" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>